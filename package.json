{
  "name": "trendix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "npm install && npm install -g nodemon && npm install -g express && npm install -g mongoose && npm install -g multer && npm install -g nodemailer && npm install -g openai && npm install -g bcrypt && npm install -g cloudinary && npm install -g cookie-parser && npm install -g dotenv && npm install -g jsonwebtoken"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.9.0",
    "bcrypt": "^5.1.1",
    "cloudinary": "^2.6.1",
    "cookie-parser": "^1.4.7",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "form-data": "^4.0.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.14.2",
    "multer": "1.4.5-lts.2",
    "nodemailer": "^7.0.3",
    "openai": "^4.100.0"
  }
}
